\begin{tikzpicture}


 \foreach \x in {1,...,8}
  \node (GD\x) at ($(1.5*\x, 0)$) {${GD}_\x$};

 \foreach \t in {1,...,12}
  \node (T\t) at ($(0.5+\t, -2)$) {$T_{\t}$};

 \foreach \p in {1,...,6}
  \node (P\p) at ($(2*\p, -4)$) {$P_\p$};

 \foreach \r in {1,...,6}
  \node (R\r) at ($(2*\r, -5)$) {$R_\r$};

 
 \foreach \g in {1,...,8}
  \foreach \t in {1,...,12}
   \draw (GD\g) -- (T\t);

 \foreach \t in {1,...,12}
  \foreach \p in {1,...,6}
   \draw (T\t) -- (P\p);

 \foreach \p in {1,...,6}
  \foreach \r in {1,...,6}
   \draw (P\p) -- (R\r);       
   


 \draw[red, line width=0.25mm]    (GD3) -- (T5) -- (P1) -- (R6);
 \draw[red, line width=0.25mm]    (GD3) -- (T1) -- (P3) -- (R4);
 \draw[red, line width=0.25mm]    (GD3) -- (T11) -- (P5) -- (R4);

 \draw[blue, line width=0.25mm]   (GD6) -- (T8) -- (P1) -- (R6);
 \draw[blue, line width=0.25mm]   (GD6) -- (T8) -- (P4) -- (R2);
 \draw[blue, line width=0.25mm]   (GD6) -- (T6) -- (P2) -- (R3);

 \draw[green, line width=0.25mm]  (GD8) -- (T11) -- (P6) -- (R2);
 \draw[green, line width=0.25mm]  (GD8) -- (T9) -- (P2) -- (R1);
 \draw[green, line width=0.25mm]  (GD8) -- (T2) -- (P2) -- (R5);

 \draw[yellow, line width=0.25mm] (GD2) -- (T5) -- (P3) -- (R2);
 \draw[yellow, line width=0.25mm] (GD2) -- (T10) -- (P6) -- (R5);
 \draw[yellow, line width=0.25mm] (GD2) -- (T7) -- (P5) -- (R2);

\end{tikzpicture}
